FROM ubuntu:24.04

RUN apt-get update && apt-get install -y \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY *.cpp *.h ./

RUN g++ -std=c++11 -o pmain.cpp src/reciever.cpp src/sender.cpp src/utils.cpp -o /app/main

ENTRYPOINT ["/app/main"]